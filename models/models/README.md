# Models

The trained model files are not stored in this repository due to file size limits.

To generate the model files, run all cells in the notebook:
notebooks/apartment_price_prediction.ipynb

This will create:
- best_model.pkl — Gradient Boosting model
- le_city.pkl — City encoder
- le_loc.pkl — Location encoder
- scaler.pkl — Feature scaler
